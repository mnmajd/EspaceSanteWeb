{% extends '::default/home.html.twig' %}


{% block content  %}


    <link rel="stylesheet" href="{{ asset('css/styleIdeal.css')}}"  type="text/css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
    <script language = "javascript">

        /* $("#loadstudent").click(function () {
             var taille = $("#taille").val();
             var poids = $("#poids").val();
             var imc = parseInt(taille) + parseInt(poids);
             alert(imc);
             $("#rslt").val(imc);
         })

        // var taille = parseInt();
       var taille = valueOf(parseFloat($('#taille')));
        var poids = valueOfparseInt($('#poids'));
        var imc = poids / (taille * taille);
        */
        $(document).ready(function() {


            $("#calcul").on("click", function (event) {

                var taille = $("#taille").val();


                if ($("#f").is(':checked')) {
                    var poidF = parseFloat(taille) - 100 - ((parseFloat(parseInt(taille) - 150)) / 2.5);
                    var p = poidF.toFixed(1);


                }
                else if ($("#h").is(':checked')) {
                    var poidH = parseFloat(taille) - 100 - ((parseFloat(parseInt(taille) - 150)) / 4);
                    var p = poidH.toFixed(1);
                }

                $.ajax({
                    url: 'imc',
                    type: 'POST',

                    success: function (response) {


                        $("#rslt").html("Votre poids est excessif à partir de "+ p + "Kg");
                        //$("#rslt").html( "VOTRE POIDS IDÉAL SELON LA FORMULE DE LORENTZ");


                    },
                    error: function (xhr, textStatus, errorThrown) {
                        alert('Ajax request failed.');
                        $('#rslt').append('no');
                    }
                });
            });


            $("#calcul2").on("click", function (event) {

                var taille2 = $("#taille2").val();
                var age = $("#age").val();


                if ($("#min").is(':checked')) {

                    var mince = (parseFloat((taille2)) - 100 + (parseFloat(age)) / 10) * 0.9 * 0.9;
                    var normal = mince.toFixed(1);
                }

                  else if ($("#moy").is(':checked')){
                     var moyen = (parseFloat((taille2)) - 100 + (parseFloat(age)) / 10) * 0.9  ;
                      var normal = moyen.toFixed(1);
                  }



                  else if ($("#lar").is(':checked')) {
                      var large = (parseFloat((taille2)) - 100 + (parseFloat(age)) / 10) * 0.9  * 1.1 ;
                      var normal = large.toFixed(1);
                  }


                $.ajax({
                    url: 'poidsideal',
                    type: 'POST',

                    success: function (response) {


                        $("#rslt2").html("Votre poids idéal est de "+ normal + " Kg");
                        //$("#rslt").html( "VOTRE POIDS IDÉAL SELON LA FORMULE DE LORENTZ");

                    },
                    error: function (xhr, textStatus, errorThrown) {
                        alert('Ajax request failed.');
                        $('#rslt').append('no');
                    }
                });
            });
        });



    </script>
    <br> <br> <br>
    <h1>Poids idéal </h1>

<br>

    <p id="def">
    Le poids idéal n’existe pas ! Eh oui, s’il existe effectivement plusieurs échelles permettant de l’évaluer selon divers critères,
    le plus important reste évidemment d’être bien dans sa peau… Hormis l’indice de masse corporelle (IMC),
    le seul validé par l’Organisation mondiale de la santé (OMS),
    il existe d’autres indices plus personnalisés prenant en compte d’autres variables pour calculer son poids idéal.
    <br>
    </p>

    <br>

    <br>
    <h1> LE POIDS IDÉAL SELON LE SEXE </h1>
    <br>

    <div id="p">
    <div class="inputs" align="center">
        <br>
        <form class="form-style-7">



        <input type="number" id="taille" placeholder="Votre taille en centimètres"> <br> <br>
        <label> Vous etes </label>
    <form>
        <input type="radio" id="f" value="femme" name="genre"> Une femme
        <input type="radio" id="h" value="homme" name="genre"> Un Homme <br>

        </form>
        <br>
        <a href="#" id="calcul"> <img src="{{ asset('images/cal.png') }}"  width=65 height=70/> </a>

        </form>

    </div>
        <br> <br>
        <div id="rslt" align="center">
<br>
        </div>
    </div>


    <br>




<br> <br> <br> <br>

    <h1> LE POIDS IDÉAL SELON LA MORPHOLOGIE </h1>
    <br>

    <br>
    <p id="def2"> Selon que vous soyez "mince", "normal" ou "large", votre poids idéal selon la formule de Creff différera.
        Là encore, les résultats sont purement indicatifs, d’autant plus que la notion de morphologie reste subjective </p>

<br> <br>
    <h2> AVEC LA FORMULE DE CREFF </h2>
    <br>
    <div id="p">
        <div class="inputs" align="center">
            <br>
            <form class="form-style-7">


            <input type="number" id="taille2" placeholder="Votre taille en centimètres"> <br> <br>

    <input type="number" id="age" placeholder="Votre age"> <br> <br> <br>
    <label> Vous etes (votre morphologie): </label>
    <form>
        <input type="radio" id="min" value="mince" name="t"> Mince
        <input type="radio" id="moy" value="moyen" name="t"> Moyen
        <input type="radio" id="lar" value="large" name="t"> Large <br>
        <br>
    </form>

                <a href="#" id="calcul2"> <img src="{{ asset('images/cal.png') }}"  width=65 height=70/> </a>
            </form>
        </div>
            <br>

            <div id="rslt2" align="center">

            </div>
            <br>
        </div>

        </div>

    </div>
        <br>
        </div>
        <br>
    </div>


    <br>
    <br>



{% endblock %}