{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categoriearticle</h1>

    <table>
        <tbody>
            <tr>
                <th>Nomcat</th>
                <td>{{ categorieArticle.nomCat }}</td>
            </tr>
            <tr>
                <th>Idcat</th>
                <td>{{ categorieArticle.idCat }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categoriearticle_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categoriearticle_edit', { 'idCat': categorieArticle.idCat }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
