{% extends 'base.html.twig' %}

{% block body %}
    <h1>Membresdefis list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for membresDefi in membresDefis %}
            <tr>
                <td><a href="{{ path('membresdefis_show', { 'id': membresDefi.id }) }}">{{ membresDefi.id }}</a></td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('membresdefis_show', { 'id': membresDefi.id }) }}">show</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    
{% endblock %}
