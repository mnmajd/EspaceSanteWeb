{% extends 'Admin/admin.html.twig' %}

{% block body %}
    <h1>Questionquiz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ questionQuiz.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ questionQuiz.question }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('questionquiz_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('questionquiz_edit', { 'id': questionQuiz.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
